# Render configuration
services:
  - type: web
    name: genzshop-backend
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: DB_NAME
        value: genzshop
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://genzshopa.vercel.app,http://localhost:3001,http://localhost:5174
      - key: PORT
        value: 80
